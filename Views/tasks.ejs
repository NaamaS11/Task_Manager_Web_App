<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Tasks</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-light">
    <div class="container mt-5">
      <h1 class="text-center mb-4">All Tasks</h1>

      <div class="text-center mb-4">
        <a href="/tasks/add" class="btn btn-primary">Add Task</a>
      </div>

      <div class="table-responsive">
        <table class="table table-bordered table-striped text-center">
          <thead class="table-dark">
            <tr>
              <th>ID</th>
              <th>Status</th>
              <th>Context</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% tasks.forEach(task => { %>
            <tr>
              <td><%= task.ID %></td>
              <td>
                <% if (task.taskStatus === 'complete') { %>
                <span class="badge bg-success">Complete</span>
                <% } else { %>
                <span class="badge bg-warning text-dark">Incomplete</span>
                <% } %>
              </td>
              <td><%= task.task_context %></td>
              <td>
                <form
                  action="/tasks/<%= task.ID %>/complete?_method=PUT"
                  method="POST"
                  class="d-inline"
                >
                  <button type="submit" class="btn btn-success btn-sm">
                    Mark as Complete
                  </button>
                </form>

                <form
                  action="/tasks/<%= task.ID %>?_method=DELETE"
                  method="POST"
                  class="d-inline"
                >
                  <button type="submit" class="btn btn-danger btn-sm">
                    Delete
                  </button>
                </form>
              </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  <div class="text-center mt-4">
    <a href="/logs" class="btn btn-info">View Logs</a>
  </div>
</div>
    <footer>©All rights reserved to Rachel Adi Levon</footer>
  </body>
</html>
